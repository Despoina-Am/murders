anaylizing the us gun murders based on FBI data.

download-data.R downloads cvs file to data directory
wrangle-dara.R creates a derived dataset and saves as R object in rda directoty
analysis.R generates a plot and saves it to the figs directory